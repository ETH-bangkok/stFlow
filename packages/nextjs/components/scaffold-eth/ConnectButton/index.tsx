"use client";

import { DynamicWidget } from "@dynamic-labs/sdk-react-core";

/**
 * Wagmi dynamic Connect Button (watch balance + custom design)
 */
export const DynamicConnect = () => {
  return <DynamicWidget />;
};
